字典：
	字典是无序的键值对（{key:value}）集合，在同一个字典里键必须是互不相同的。初始化字典时，在大括号内放置一组逗号分隔的键值对，这是字典的输出方式。我们通常使用键来检索存储在字典中的数据。
	创建新的键值对很简单：字典名｛key｝=value
	使用del关键字删除任意指定的键值对：del 字典名｛key｝
	使用in关键字查询指定的键是否存在于字典中：value in 字典名
	
	必须知道：
	字典中的键必须是不可变类型，不能使用列表作为键；dict()可以从包含键值对的元组中创建字典。
	items()-->遍历一个字典
	向字典中的元素添加数据，我们首先要判断这个元素是否存在，不存在则创建一个默认值。可以使用dict.setdefault(key,default)查找更有效率。如果索引一个不存在的键，会抛出keyError错误。我们可以使用dict.get(key,default)来索引键，不存在时，返回指定的default.
	遍历列表的同时获得元素的索引值-->enumerate()
	同时遍历两个序列类型，可以使用zip()函数。

