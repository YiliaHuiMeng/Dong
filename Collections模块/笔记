	1.Counter模块：是一个有助于hashable对象计数的dic是一个无序的集合，其中hashable对象的元素存储为字典的键，他们的计数存储为字典的值，计数可以为任意整数，包括零和负数。
	查看Counter的帮助信息：help(Collections.Counter.__doc__)
	该对象有一个叫做elements()的方法，其返回的序列中，依照技术重复元素相同次数，元素顺序是无序的。
	most_common()方法返回最常见的元素及其计数，顺序为最常见到最少。
	
	2.defaultdict是内建dict类的子类，它覆写了一个方法并添加了一个可写的实例变量。其余功能与字典相同。
	defaultdict()第一个参数提供了default_factory属性的初始值，默认值为None，default_factory属性值将作为字典的默认数据类型，所有剩余的参数与字典的构造方法相同，包括关键字参数。
	同样的功能，使用defaultdict比使用dict.setdefault方法快。

	3.namedtuple模块：命名元组有助于对元组每个位置赋予意义，并且让我们的俄代码有更好的可读性和自文档性。你可以在任意使用元组地方使用命名元组。

